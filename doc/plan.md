# Claudio FM — 个人 AI 音乐电台 需求文档

## 项目描述
复刻 Claudio 个人 AI 电台系统：读懂听歌习惯 → 规划声音 → 像 DJ 那样播报

## 核心功能
1. 播放器界面（PWA，可安装到手机和桌面）
2. AI DJ 播报员（DeepSeek 驱动，轻松复古风格）
3. 音乐播放（网易云音乐 NeteaseCloudMusicApi）
4. 浏览器内置 TTS 语音播报（Web Speech API）
5. WebSocket 实时状态同步
6. SQLite 本地状态持久化
7. 与 DJ 聊天功能

## 技术架构（四层）

### 第一层 — 外部上下文
- **用户品味语料**：user/taste.md、user/routines.md
- **BRAIN**：DeepSeek API（OpenAI 兼容接口）
- **MUSIC**：NeteaseCloudMusicApi（子进程，端口 3001）
- **VOICE I/O**：浏览器 Web Speech API（TTS）

### 第二层 — 本地大脑（Node.js）
- `server/index.js` — Express 主服务 + WebSocket
- `server/music.js` — 网易云 API 封装
- `server/ai.js` — DeepSeek 适配器
- `server/context.js` — 提示词组装
- `server/stats.js` — SQLite 状态记忆

### 第三层 — 运行时拼合
- Context Window = dj-persona + 用户品味 + 时间环境 + 历史记录
- Model → {say, play[], reason} → TTS 合成 → WS 推送

### 第四层 — 交互表层
- Progressive Web App（端口 8080）
- 复古电台 UI 设计
- HTTP 接口：POST /api/chat、GET /api/now、GET /api/next、WS /stream

## 用户需求确认（2026-05-17）
- 大模型：DeepSeek（需配置 DEEPSEEK_API_KEY）
- 音乐源：网易云音乐免费 API
- TTS：浏览器内置（免费，无需 Key）
- 部署：PWA（可安装到手机）
- DJ 风格：轻松复古风格

## 设计风格
- 复古电台美学：暗色系（深棕/黑）+ 金色/琥珀色
- 黑胶唱片旋转动画
- DJ 对话框（打字机效果）
- 复古字体和控件

## API 接口
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/now | 当前播放状态 |
| POST | /api/next | 切换下一首 |
| GET | /api/music/url/:id | 获取音乐播放地址（代理） |
| GET | /api/music/lyric/:id | 获取歌词 |
| GET | /api/music/search | 搜索音乐 |
| POST | /api/chat | 与 AI DJ 对话 |
| GET | /api/history | 播放历史 |
| WS | /stream | 实时状态推送 |

## 已完成（2026-05-17 更新）

- [x] 基础播放器 UI（黑胶唱片、进度条、音量、播放控制）
- [x] AI DJ 播报（DeepSeek）
- [x] 与 DJ 聊天 + 点歌（支持"艺术家的歌名"格式）
- [x] QQ 音乐集成（搜索 + URL + 歌词）
- [x] CDN probe 机制（getSongUrl 验证后再返回）
- [x] QQ→网易云 fallback（指定艺术家 / 未指定艺术家 两种策略）
- [x] 服务端 Edge TTS（zh-CN-YunyangNeural 男声播音员，替换 Web Speech API）
- [x] 歌词同步显示（LRC 解析 + 自动滚动）
- [x] Service Worker 缓存修复（JS/CSS 不缓存）
- [x] userRequested 用户点歌错误处理（不自动跳歌，弹出提示）
- [x] 试听片段检测（≤35s 自动跳下一首，仅对网易云）
- [x] PWA 可安装（manifest + Service Worker）
- [x] 网易云音乐歌单同步到本地 `data/playlists.json`
- [x] QQ 音乐歌单同步到本地 `data/playlists.json`
- [x] QQ 音乐歌单接口适配：支持用户创建歌单列表与歌单详情拉取
- [x] 播放器改版为 Claudio 暗色电台模板
- [x] 播放态频谱屏：播放时显示居中呼吸音柱，停止/暂停时隐藏频谱并显示时间日期
- [x] 音量调节功能
- [x] 底部 DJ 快捷输入框接入 `/api/chat`
- [x] DJ 头像统一为 `public/avatar.jpg`
- [x] TTS 声音参数环境变量化（voice/rate/pitch/volume）
- [x] 启动端口占用自动回退（8080 忙时尝试 8081+）
- [x] 全量歌单歌曲按类型重新分类，生成 `data/categories.json` 与 `data/songs-classified.json`
- [x] 新增 `npm run classify`，后续可基于分类播放队列实现“按类型播放”
- [x] 用户品味自动学习：根据近期收听历史提取高频歌手/类型/歌曲，注入 DJ 上下文并影响本地推荐排序
- [x] 天气 API 上下文：可选配置高德天气，将天气信息注入 DJ 播报；OpenWeather 作为兜底，未配置 Key 时自动跳过
- [x] 搜索结果去重：按歌曲名 + 艺人过滤重复候选，减少同曲多版本干扰
- [x] 语音输入 MIC：接入浏览器 `SpeechRecognition` / `webkitSpeechRecognition`，识别后直接发送 DJ 指令
- [x] 歌单导入日志可视化：导入完成后在 UI 展示网易云、QQ 音乐、总歌曲数和日志
- [x] 按类型播放：从 `data/categories.json` 读取分类，支持“播放国风”“来点 KTV”“切到日语”等指令
- [x] 顶部天气可视化：顶部时间下方显示北京天气，格式精简为“北京市，阴，22°C”
- [x] 国内天气 API 切换：高德天气作为主天气源，OpenWeather 保留为兜底
- [x] DJ 播报音色调整：支持从 `.env` 读取 `TTS_VOICE`、`TTS_RATE`、`TTS_PITCH`、`TTS_VOLUME`，当前使用 `zh-CN-YunxiNeural`
- [x] 版权失败/试听片段切歌体验优化：自动切歌时立即停止旧 DJ 打字机和 TTS，避免上一首播报继续朗读
- [x] 端口占用服务端回退：8080 被占用时自动尝试后续端口，并在控制台输出实际访问地址
- [x] 播放器截图回归：新增 Playwright 视觉回归脚本，覆盖桌面/移动端的播放态与暂停态，生成 `data/screenshots/*.png` 并做基础布局/非空截图断言
- [x] 点歌延迟优化：QQ URL 解析新增 circuit breaker，连续失败后短时间跳过 QQ 候选，避免每首歌都等待 CDN probe
- [x] 播放历史可视化界面：新增底部 `HIST` 面板，展示播放记录数、去重歌曲、常听歌手、常听类型和最近播放列表
- [x] 喜欢 / 不喜欢反馈系统：新增 `LIKE` / `LESS` 控件与自然语言反馈指令，记录本地偏好并影响推荐排序
- [x] 场景电台模式：新增深夜、工作、通勤、下雨、睡前、回忆杀、KTV 等场景队列，复用分类数据并接入偏好排序
- [x] DJ 播报策略控制：场景自动切换 normal/minimal/short/warm/silent 策略，支持“少说话”“多介绍一点”“只播歌”“恢复播报”等自然语言指令
- [x] 队列预览和轻量编辑：`QUEUE` 区域展示接下来 5 首，支持删除下一首、重新生成队列和插队点歌
- [x] 每日 / 时段自动电台简报：结合时间、天气、作息、近期收听和偏好生成启动开场，同一天同一时段复用缓存

## 改进计划（待办）

### 下一阶段产品路线
目标：把 Claudio FM 从“会说话的播放器”推进为“懂场景、会记忆、可轻量调教的私人音乐电台”。

核心用户需求：
- 少操作：打开就能听，点歌和切歌不需要复杂配置。
- 符合当下状态：根据时间、天气、情绪、工作/休息场景自动调整音乐。
- 有陪伴感：DJ 不只是报歌名，而是用合适的频率和语气陪伴。
- 不被打断：版权失败、切歌延迟、TTS 错位、音量突变都要尽量被系统自动消化。
- 可控但不复杂：支持“多放这个”“别放这类”“安静一点”“来点深夜模式”这类自然语言控制。

推荐实施顺序：
1. **喜欢 / 不喜欢反馈系统**：建立偏好闭环，记录“喜欢、少放、屏蔽、完整听完、频繁跳过”等信号。
2. **场景电台模式**：在现有类型播放上升级为深夜、工作、通勤、下雨、睡前、回忆杀、KTV 等场景。
3. **DJ 播报策略控制**：按场景控制 DJ 说话频率、长度和语气，例如工作时少说、深夜可多一点陪伴。
4. **队列预览和插队**：展示接下来几首歌，支持删除下一首、插队点歌、重新生成队列。
5. **每日 / 时段自动电台**：结合 `user/routines.md`、天气和历史偏好生成每日开场与时段推荐。

### 高优先级
- [ ] **QQ 音乐 VIP 账号（外部条件）**：需要换用有完整 VIP/SVIP 权限的 Cookie，才能彻底解决 QQ CDN 404；当前代码已通过 CDN probe、QQ→网易云 fallback、分类播放优先网易云 ID 缓解，但不能替代账号权限
- [ ] **端口提示优化（部分完成）**：服务端端口自动回退与控制台实际地址输出已完成；仍需前端或启动脚本自动打开实际端口地址，减少用户看错端口
- [x] **喜欢 / 不喜欢反馈系统**：支持用户用按钮或自然语言表达“喜欢这首”“少放这个歌手”“别放这类”，并写入本地偏好数据影响推荐
- [x] **场景电台模式**：支持“深夜模式”“工作专注”“通勤提神”“下雨安静”“睡前低刺激”等场景队列
- [x] **DJ 播报策略控制**：按场景和用户指令控制 DJ 是否多说、少说、只播歌、开场白和切歌短句

### 中优先级
- [x] 用户品味自动学习（根据收听历史调整推荐）
- [x] 添加天气 API（高德天气优先，OpenWeather 兜底）注入 DJ 播报（"今天下雨，适合听一首..."）
- [x] 搜索结果去重（同名不同版本过滤，只保留最佳版本）
- [x] 语音输入 MIC 接入真实浏览器录音/识别流程
- [x] 歌单导入日志可视化，在 UI 中展示网易云/QQ 导入成功数和日志
- [x] 按类型播放：从 `data/categories.json` 读取分类，支持“播放国风”“来点 KTV”“切到日语”等指令
- [x] **队列预览和轻量编辑**：展示接下来 5 首，支持删除下一首、插队点歌、重新生成队列
- [x] **每日私人电台简报**：打开时根据时间、天气、近期收听和用户作息生成一句开场
- [ ] **播放失败产品化提示**：版权失败时用轻提示和静默换源降低打断感，并记录短期不可播歌曲

### 低优先级
- [ ] 添加 UPnP 推送到家庭音响
- [ ] 添加飞书日历读取功能
- [x] 播放历史可视化界面
- [ ] 睡眠定时与渐弱停止
- [ ] 闹钟电台
- [ ] 今日听歌报告
- [ ] 本地缓存可播歌曲
